import 'package:flutter_test/flutter_test.dart';

void main() {
  // Widget tests are defined in test/widget/ directory.
  // This file is a placeholder — the default counter test was removed
  // because the app no longer has a counter widget.
  test('placeholder', () => expect(true, isTrue));
}
